{
  "name": "json-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "host": "npx json-server db.json",
    "server": "json-server --watch db.json --host 0.0.0.0 --port 3000",
    "web": "npx http-server . --port 8080 --host 0.0.0.0"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "nanoid": "^5.0.8"
  }
}
